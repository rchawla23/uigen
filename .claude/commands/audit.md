Your goal is to update any vulnerabilities

Do the following:
1. Run `npm audit` to fix vulnerable installed packages in this project
2. Run `npm audit fix` to apply updates
3. Run tests and verify that updates didn't break anything